# Implementation of DsP-YOLO

Implementation of DsP YOLO as given in the paper _DsP-YOLO: An anchor-free network with DsPAN for small object detection of multiscale defects_

Code includes few variants not included in the paper. 
`pip install ultralytics` to run. `train.ipynb` does transfer learning (fine-tuning) on the given dataset
